-

# Bookforge

Book management workspace built with Go.

## Run the backend

```bash
go run ./02_backend/cmd/api
```

The health endpoint is available at `http://localhost:8080/health`.

